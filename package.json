{
  "name": "test-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build",
    "start": "node server.js"
  },
  "engines": {
    "node": "14.16.0"
  },
  "keywords": [],
  "author": "Connor DiPietro",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.17.2",
    "dotenv": "^5.0.0",
    "express": "^4.15.3",
    "mongoose": "^4.11.5",
    "path": "^0.12.7"
  }
}
